@model List<ORM.Models.Department>
<h2>Index</h2>
<a href="/Depa rtment/Create" class="btn btn-primary">Create</a>
<br /><br />
<table class="table table-bordered">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Action</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td><a href="/Department/Edit/@item.Id" class="btn btn-info">Edit</a>
            <a href="/Department/Details/@item.Id" class="btn btn-primary">Details</a>
             <a href = "/Department/Delete/@item.Id" class="btn btn-danger">Delete</a></td>
        </tr>

    }
</table>
